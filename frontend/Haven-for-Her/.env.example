# Backend API URL (used by Vite proxy in development, or as base URL in production)
# In production, set this when the frontend host does not proxy /api requests to the backend.
VITE_API_BASE_URL=https://localhost:7229
